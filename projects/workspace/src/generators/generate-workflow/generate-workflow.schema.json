{
  "$schema": "https://json-schema.org/schema",
  "$id": "GenerateWorkflow",
  "title": "Generate Workflow Options",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Alfred workflow name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}
